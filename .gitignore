# .gitignore
# Purpose: Prevent sensitive files, build artifacts, logs, system/IDE noise, and dependencies from being committed.
# Why: Keeps the repository clean, reduces size, prevents merge conflicts, and protects secrets.

# --- Dependencies (DO NOT COMMIT - reinstall with npm install) ---
node_modules/
client/node_modules/
package-lock.json
client/package-lock.json

# --- Environment variables (NEVER commit real secrets) ---
.env
.env.txt

# --- Build outputs / caches ---
dist/
build/
client/build/
frontend/client/
.cache/
.parcel-cache/
.next/
.nuxt/
.vite/
coverage/
storybook-static/
cache/
out/
client/.cache

# --- Logs ---
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# --- User uploaded files (store locally, not in repo) ---
uploads/
uploads/evidence/
uploads/profiles/

# --- Temporary files ---
tmp/
temp/
query

# --- OS files ---
.DS_Store
Thumbs.db

# --- IDE/editor settings ---
.idea/
.vscode/
*.swp
*.swo
*.iml
.project
.classpath

# --- Misc ---
*.local
*.env.*.local
test-results/
.env.development.local
.env.test.local
.env.production.local


node_modules/
node_modules/
node_modules/
