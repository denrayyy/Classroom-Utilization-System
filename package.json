{
  "name": "classroom-utilization-system",
  "version": "1.0.0",
  "scripts": {
    "server": "cd backend && npm run server",
    "client": "cd frontend && npm start",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "install-all": "cd backend && npm install && cd ../frontend && npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}